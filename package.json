{
  "name": "map-collector",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-config.js",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
